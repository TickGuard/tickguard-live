# Tick Risk Backend (MVP)

FastAPI backend for homeowner tick-risk scoring MVP.

## Run locally

1. Create `.env` from `.env.example`
2. Install dependencies:

   ```bash
   pip install -r requirements.txt
